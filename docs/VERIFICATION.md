# Фактическая проверка 23.09.2026

## Разделение Frontend / Backend / Logic

Рабочее дерево `codex/split-agent-responsibilities` от `0ea01e6`, без нового коммита.
В этой копии создана `.venv`, зависимости установлены из `requirements.lock`.

- Полный pytest: **52 passed in 31.64s** (46 прежних сценариев и 6 новых проверок application).
- Smoke: 4 позиции, 2 поставщика, ручной ноль, approved, CSV 6406 bytes / XLSX 8495 bytes.
- pip check: ошибок зависимостей нет; git diff --check: ошибок пробелов нет.
- AppTest через `app.py`: демо, расчёт, утверждение, изменение опций отменяет расчёт/утверждение.
- Backend: копирование входов, неудачный импорт, повторный импорт поставщика, сохранение второго, provenance правок и очистка таблицы редактором.
- Браузер на 127.0.0.1:8501: демо, 4 позиции / 2 поставщика, сообщение локального утверждения, доступны CSV/XLSX.
- Скачивание через браузер повторно не проверялось; содержимое форматов проверено тестами и smoke.
- Отдельная чистая копия нового коммита не проверялась: изменения ещё не закоммичены. Реальных архивов нет.

## Историческая Проверка MVP

Код проверенного commit: `541a8c4f5faeb8034828309bfe65bc975d734237`. Проверка завершена около 14:18 UTC+5. Последующее изменение этой записи/STATUS не меняет проверенный код.

## Чистый запуск

Из commit создан архив командой `git archive --format=zip`, распакован в отдельную новую папку без `.venv`, загруженных данных или конфигурации пользователя. Выполнены команды из README:

```powershell
.\setup.ps1
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe scripts/smoke.py
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m streamlit run app.py --server.port 8502
```

- setup: успешно создана новая `.venv`, установлены точные версии из requirements.lock; найден комплектный Python 3.12.14. Личный API-ключ или аккаунт для расчёта не использовался.
- pytest: **46 passed in 13.11s**.
- smoke: mode=synthetic, positions=4, suppliers=2, manual_zero=0.0, approval=approved, CSV 6406 bytes / XLSX 8496 bytes. Размер XLSX может немного меняться из-за метаданных контейнера; числовой результат детерминирован.
- pip check: **No broken requirements found**.
- `http://127.0.0.1:8502/_stcore/health`: **ok**. После проверки временный сервер остановлен.

## Содержательная проверка

- T1/T2/T3: 65→70, 45→50, нулевая потребность не создаёт MOQ-заказ; минимум и кратность различаются.
- M1: независимые изменения истории, срока, плана роста, категории, запаса и пути меняют результат в ненасыщенных примерах.
- M2: повторяющийся июньский пик сохраняется; устойчивый рост повышает прогноз; один всплеск после исключения не раздувает базу; будущий prior не используется.
- M3: 200 наблюдаемых единиц за 20 доступных дней + 10 подтверждённых дней → 300; пересечения не удваиваются; частичные продажи не удваиваются; все дни stockout без доступных наблюдений блокируют точную оценку.
- M4: одиночный 1000 исключается, дробление заказа клиента на документы обнаруживается, регулярные крупные клиенты и массовый пик сохраняются; document_id не подменяет customer_id.
- M5: одинаковый SKU у двух поставщиков разделён, есть единица, источник, срочность и объяснение.
- Импорт: исходный знак расходной накладной, покупки и заказы не продажи, текущий/исторический остаток, отсутствие двойного резерва/истории/складского запаса, неизвестные/поздние ETA, дубли партий, границы дат, коды с нулями.
- 12 синтетических Excel-макетов проверяют ветки шести типов отчётов IEK/Systeme; V2 использует AX/AY/AZ и BC, не сомнительные AP/AQ/AR/AS.
- Экспорт: CSV BOM и XLSX с типизированными числами/текстовыми SKU, обезвреживание текстовых формул; ручной ноль; изменение входов и правок отменяет утверждение.
- Streamlit AppTest: загрузка демо, расчёт, четыре позиции, утверждение, изменение переключателя, удаление устаревшего результата/утверждения.

## Реальный браузер

На рабочем интерфейсе 8501 дополнительно вручную автоматизирован браузерный сценарий:

1. Открыта панель данных, загружено демо, выполнен расчёт; показаны поставщики, фильтры склада и категории.
2. Утверждены выбранные строки.
3. У IEK / 000001_ количество в редакторе изменено с 90 на **0**. В таблице виден ноль; интерфейс сообщил об отмене утверждения.
4. Введена причина «Синтетическая проверка: закупка не требуется», выполнено повторное утверждение. Видно подтверждение локального состояния и обе кнопки экспорта.

Выгруженные байты CSV/XLSX проверены расчётным workflow-тестом (включая открытие XLSX через openpyxl). Совместимость с установленным Microsoft Excel или конкретным шаблоном 1С не заявляется.

## Непроверенное

Оригинальные архивы не предоставлены: реальная сверка, производительность полного каталога, качество прогноза и денежная экономия не измерены. Linux/macOS и многопользовательская эксплуатация не проверены. Отправок заказов, деплоя и доступа к внешним LLM нет.
