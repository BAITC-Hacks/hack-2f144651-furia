# UI-02: две темы и удобства рабочего экрана

- Статус: done — реализовано и проверено; подтверждение push в итоговом сообщении.
- Поручение: обновление дизайна, light/dark и полезные Frontend-функции; референсы Inspo.
- Исполнитель Frontend и интегратор: Codex `/root`, сессия
  `01a0ce1a-209b-73c1-9da8-684b13a451e1`.
- Рабочая папка: `C:\Users\User\OneDrive\Документы\ChatGPT\Хакалаем`, общее дерево main.
- База: `54f9bff8ef59f2866cfe680d5c3ace91737f5efd`, после fetch/ff-only.
- Передача: интегратор AUD-01 явно передал Git, TASKS и STATUS после push.
  UI-LOGIC-01 уже интегрирован в базу; его расчёты и статусы сохраняются.
- Точные файлы: `src/ekt_ui/app.py`, `src/ekt_ui/layout.py`,
  `src/ekt_ui/styles.css`, `src/ekt_ui/results.py`, `src/ekt_ui/review.py`,
  новый `src/ekt_ui/workspace.py`, `.streamlit/config.toml`,
  новый `tests/frontend/test_workspace.py`, `docs/DASHBOARD.md`,
  `docs/handoffs/frontend.md`, `docs/tasks/UI-02.md`, `docs/TASKS.md`, `docs/STATUS.md`.
- Зависимости: существующие Bundle/Calculation, state, filter_orders, review API.
  Новых библиотек и контрактов ekt нет. AUD-FE-01 целиком не назначается.
- Контракт: сохранённые фильтры и плотность действуют в текущей сессии;
  темы переключаются нативными настройками Streamlit, включая таблицы и графики.
  Изменение представления не изменяет количества, выбор, подпись и экспорт.
- Приёмка: обе темы; пустой экран и шаги; быстрые и сохранённые фильтры,
  восстановление фильтров при смене набора; компактная таблица; ручной ноль,
  сохранение утверждения при изменении вида; браузер desktop/mobile.
- Проверки: frontend pytest, полный pytest, smoke, browser, diff --check.
- Ограничения: постоянное хранение, цены/бюджеты и сценарии — будущие задачи
  Backend/Logic. Не имитировать готовые функции. Оригиналы не проверяются.
- Итоговые результаты и публикация: заполняются после проверок.

## Проверенный результат

Светлая/тёмная/системная темы, новый стартовый экран, полоса этапов,
быстрые подборки, восемь сохранённых представлений в сессии и плотность таблицы.
Область склада видна в заказах. Backend/Logic API и формулы не менялись.
Референсы и задания следующего этапа — [DASHBOARD](../DASHBOARD.md).

Среда: Windows, Python 3.12.14, Streamlit 1.64.0; зависимости не менялись.

| Проверка | Результат |
|---|---|
| `.venv/Scripts/python.exe -m pytest tests/frontend -q` | 68 passed, 186.69s; до добавления нового файла тестов, предупреждение о доступе к pytest cache |
| `.venv/Scripts/python.exe -m pytest tests/frontend/test_workspace.py -q` | 5 passed, 26.83s; предупреждение о доступе к pytest cache |
| `.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider` | 308 passed, 20 skipped, 240.51s; итоговые Python-изменения, включая обработку отсутствующего поставщика |
| `.venv/Scripts/python.exe scripts/smoke.py` | synthetic, 4 позиции / 2 поставщика, manual_zero=0, approved, CSV/XLSX созданы |
| Браузер localhost:8503 | Light/Dark, демо, расчёт, сохранение представления; нативная таблица и диалог; desktop 1280×720 и mobile 390×844 |
| Мобильная компоновка | документ 390px, фильтры в одну колонку, KPI 2×2, диалог в ширину экрана |
| `git diff --check` | без ошибок |

Визуальная проверка обнаружила белый фон портального диалога в Dark;
исправлено наследованием нативной поверхности и ограничением custom CSS
основным контейнером. После исправления фон диалога `rgb(17,26,22)`,
текст `rgb(229,238,234)`; повторно проверены desktop/mobile.
Тесты включают сохранение ручного нуля, Approval и полного CSV при фильтрах
и смене плотности; пустые/устаревшие фильтры не расширяются до всех позиций.

20 skipped — приватные оригиналы, не успешная их приёмка. Нагрузочный прогон
и чистая установка в этой задаче не повторялись. Независимый Reviewer отдельно
не назначался. Удалённый CI не запускался (см. CI-02/AUD-01). Известные проблемы
аудита не заявляются исправленными. Деплой и официальная сдача не выполнялись.
