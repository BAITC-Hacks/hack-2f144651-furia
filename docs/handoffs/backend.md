# Передача Backend: BE-01, BE-02 и проверка оригиналов BE-03

## AUD-BE-01: назначение, результат и передача

- Статус: D01/D02/D11 реализованы и проверены; D08 воспроизведён и исправлен
  интегратором. Итоговый общий набор: **419 passed, 20 skipped**, smoke успешен.
  Opt-in сверка оригиналов заблокирована: `EKT_PARTNER_ARCHIVE_DIR` не задан.
  Commit/push выполняет согласованный интегратор после остановки записей.
- Поручение: устранить D01/D02, предложить и реализовать backend-часть D11,
  воспроизвести D08 для интегратора; локальную проверку оригиналов оставить
  заблокированной, если исправленных архивов нет.
- Исполнитель Backend: Codex `/root`, текущая сессия.
- Сессия Backend: `01a0ce22-94d1-77d1-b48b-8900c194f46d`. По поручению
  пользователя продолжение делегировано субагентам на той же базе:
  `/root/export_review` — `src/ekt/export.py` и новый
  `tests/backend/test_export_audit_edges.py`; `/root/import_review` —
  `src/ekt/ingest.py`, `src/ekt/application.py`, `src/ekt/partner.py` и новый
  `tests/backend/test_import_budget_edges.py`. Они проверяют граничные случаи
  D01/D11 и исправляют подтверждённые дефекты. Git выполняет только интегратор;
  существующие тесты, общие документы и чужие файлы им не передаются.
- D08: `/root/schema_regression` готовит только
  `tests/backend/test_schema_foreign_keys.py`; исправление `schema.py` согласовано
  с интегратором AUD-LOG-01. Git и общие TASKS/STATUS переданы единственному
  интегратору сессии `01a0ce22-beee-7300-8b19-a1a8034ab5e8`; Backend передаёт ему
  проверенные файлы отдельным списком после остановки записей.
- База после fetch/fast-forward `origin/main`: `54f9bff8ef59f2866cfe680d5c3ace91737f5efd`.
- Рабочая папка: `C:\Users\ramaz\OneDrive\Документы\ChatGPT\Хакатон`; режим: общий `main`.
- Разрешённые файлы: `src/ekt/ingest.py`, `application.py`, `partner.py`,
  `export.py`, `tests/backend/`, `docs/INPUT_GUIDE.md`, этот handoff.
- Зависимости: изменений UI не назначено; публичный Bundle не меняется.
  `schema.py` принадлежит интегратору параллельной задачи AUD-LOG-01.
  Контракты D01/D11 переданы ему; для ошибок длинного/непредставимого текста
  Frontend должен перехватывать `ValueError` вокруг `xlsx_bytes`: текущий try
  в `render_downloads` охватывает только `export_frame`.
- D11, согласованное решение для этой backend-подзадачи: общий бюджет одного
  вызова импорта — 100 МиБ входных байтов, 200 МиБ суммарного распакованного
  содержимого ZIP/XLSX, 100 входных файлов/элементов внешнего ZIP, 500 000 строк
  и 5 000 000 ячеек исходных
  таблиц (считая заголовки). Для CSV учитывается дополнение коротких строк до
  ширины таблицы. В XLSX учитываются прямоугольники всех листов, объявленные
  dimensions и промежутки до дальних ячеек. Эти пределы
  распространяются на всю пачку; Frontend следует передать тот же бюджет для
  раннего сообщения пользователю. Импорт отклоняет превышение до дальнейшей
  обработки и не изменяет `previous`.
- Приёмка: литеральные коды и настоящие пустоты одинаковы для CSV/XLSX;
  XLSX-экспорт корректно обрабатывает запрещённые XML символы только в копии,
  а непредставимые длинные ячейки дают контролируемый отказ; бюджеты и D08
  воспроизводятся безопасными малыми сценариями; исправленные оригиналы не
  предоставлены, поэтому opt-in сверка остаётся blocked.

### Реализовано

- `src/ekt/ingest.py`: XLSX читается с тем же NA-контрактом, что и CSV;
  `NA`, `NULL`, `N/A` и `000001` сохраняются текстом, пустые значения остаются
  пропусками. Добавлен общий счётчик бюджета вызова импорта; проверки размера,
  числа файлов, строк и ячеек происходят с контролируемым `ValueError`.
- `src/ekt/application.py`, `src/ekt/partner.py`: одна пачка канонических файлов
  использует общий бюджет. ZIP партнёра проверяется до чтения payload; список
  payload больше не материализуется, XLSX обрабатываются по одному. Табличный
  бюджет применяется до загрузки книги и адаптации партнёрских данных.
  Сортировка по basename ZIP сохранена; workbook закрывается и при ошибке.
  Предыдущий Bundle остаётся неизменным при отказе.
- `src/ekt/export.py`: только XLSX-копия заменяет XML-недопустимый управляющий
  символ видимой последовательностью `\xNN`. Исходный DataFrame и CSV не меняются.
  Длина 32767 проверяется после преобразований, чтобы openpyxl не обрезал текст.
  Непредставимые XML-символы U+D800–DFFF, U+FFFE и U+FFFF дают `ValueError`
  с расположением; правила действуют для заголовков, ячеек и Calculation metadata.
  Преобразование по позициям сохраняет разные строки с одинаковым индексом и
  колонки с совпадающими заголовками. Формульная защита и строковый тип SKU сохранены.
- `tests/backend/test_audit_backend_regressions.py`: тринадцать причинных сценариев
  на CSV/XLSX, пустоты, экспорт, метаданные и безопасные бюджеты.
- `tests/backend/test_export_audit_edges.py`: дополнительные регрессии границы
  длины после escaping, повторных индексов/колонок и XML Unicode.
- `tests/backend/test_import_budget_edges.py`: ранние отказы до pandas/openpyxl,
  поддельные dimensions и дальние ячейки, накопительные бюджеты, CSV длиннее
  стандартного `csv.field_size_limit`, кавычки/переносы и порядок ZIP.
- `docs/INPUT_GUIDE.md`: описаны общие лимиты и поведение краевых текстов.

### D08: передача интегратору

Минимальное воспроизведение: три строки `products` имеют один и тот же ключ
`(supplier_id, sku_1c)`, и три строки `sales` ссылаются на этот ключ. В
`schema.validate` внешний `merge` создаёт девять промежуточных строк, несмотря
на отдельную диагностику дублей товаров. Решение — membership-проверка ключей
ссылочных таблиц без many-to-many merge, с сохранением диагностики дублей
`products`. `schema.py` и
`tests/test_schema.py` остаются владельцу Integrator; второй валидатор не добавлен.
Добавлен `tests/backend/test_schema_foreign_keys.py`: до исправления **1 failed,
25 passed**; записанный размер merge — `[(3, 9)]`. После исправления все 26
проверок проходят в профильном наборе. Остальные сценарии проверяют
все пять ссылочных таблиц, известные пары, неизвестные SKU/поставщиков и попытку
сослаться на существующий SKU другого поставщика. Файл заморожен и передан
интегратору. Он исправил `schema.py`: составные ключи сверяются через
`MultiIndex.isin`, диагностические сообщения о дублях/неизвестных кодах сохранены.

### Запрос владельцу данных и ограничения оригиналов

Чтобы снять блокировку импорта, запросить новые полные архивы IEK и Systeme с
датами отчётов и подтверждёнными исправлениями:

1. Исправить 15 ячеек MOQ IEK с Excel `#N/A` / `#Н/Д`; для действительно
   неизвестного значения оставить пусто и отдельно подтвердить, означает ли
   поле minimum или multiple. Не заменять неизвестное нулём.
2. Проверить и восстановить 18 пустых количеств продаж IEK и 13 Systeme либо
   явно подтвердить, что соответствующие исходные строки не содержат продажи.
3. Сверить противоречащие кратности Systeme между колонкой E отчёта MOQ и
   колонкой D месячного отчёта; прислать подтверждённое бизнес-значение и
   исправленные ячейки обоих файлов, не назначая приоритет источнику автоматически.
4. Подтвердить общий складской пул, единицы и таблицу конверсии для входящего
   пути, а также семантику MOQ IEK. До ответа сохранять текущие источники,
   `UNSPECIFIED`, `pending` и `unconfirmed_moq`.

Исправленные архивы в этой сессии отсутствуют; приватные записи не копировались,
не выводились и не коммитились. После получения архивов требуется локальный
opt-in прогон `tests/backend/test_partner_originals.py`.

### Проверки

| Команда | Результат |
|---|---|
| `python -m pytest tests/backend/test_audit_backend_regressions.py -q` | 13 passed |
| `python -m pytest tests/backend/test_export_audit_edges.py tests/backend/test_audit_backend_regressions.py -q` | 30 passed |
| `python -m pytest tests/backend/test_import_budget_edges.py tests/backend/test_audit_backend_regressions.py tests/test_partner.py tests/test_partner_regressions.py -q` | 77 passed |
| `python -m pytest tests/backend tests/test_partner.py tests/test_partner_regressions.py -q` | 199 passed, 20 skipped, 11.60 s после D08 fix |
| D02 `excel_codes()` из `docs/audit/reproduce.py` | CSV/XLSX одинаково вернули `NA`, `NULL`, `N/A`, `000001` |
| `python -m pytest -q -p no:cacheprovider` | Единый финальный прогон интегратора: 419 passed, 20 skipped, 122.47 s; код после прогона заморожен |
| `python scripts/smoke.py` | Финальный повтор интегратора после численных правок Logic успешен: synthetic, 4 позиции, 2 поставщика, CSV 6585 байт, XLSX 8634 байт, manual_zero=0, approval=approved |
| `git diff --check` по файлам Backend | Без ошибок |

Используется `.venv/Scripts/python.exe` (Python 3.12.14). Промежуточные полные
запуски во время редактирования общей Logic-задачи не считаются окончательной
приёмкой. Старый общий `docs/audit/reproduce.py` останавливается на
`override_tolerance`, поскольку исправленный Logic требует причину ручного
изменения; D02 вызван отдельно через `runpy.run_path(...)["excel_codes"]()`.
20 opt-in проверок оригиналов пропускаются при отсутствующем
`EKT_PARTNER_ARCHIVE_DIR`; это блокировка реальных данных, не успешная сверка.
Изменения `engine.py`, `forecast.py`, `review.py`, `demand.py`, тестов Logic и
`docs/METHODOLOGY.md` принадлежат другой задаче. Интегратор разделяет её коммит и
Backend/D08 по явным спискам и согласованным изменениям `schema.py`.

### Передача на публикацию

Записи Backend остановлены. Интегратору поручены отдельный Backend/D08 commit
и push в `origin/main` после его Logic-коммита. Явный список Backend:
`src/ekt/ingest.py`, `src/ekt/application.py`, `src/ekt/partner.py`,
`src/ekt/export.py`, `tests/backend/test_audit_backend_regressions.py`,
`tests/backend/test_export_audit_edges.py`, `tests/backend/test_import_budget_edges.py`,
`tests/backend/test_schema_foreign_keys.py`, `docs/INPUT_GUIDE.md`, этот handoff.
Отдельно интегратор включает согласованный D08 diff в `src/ekt/schema.py` и
обновляет общие TASKS/STATUS/INTERFACES. Подтверждённый удалённый SHA сообщается
после push; эта запись фиксирует проверенный состав передачи. Деплой и
официальная сдача в поручение не входят. Полная приёмка реальных данных остаётся
blocked; обработка исключений `xlsx_bytes` в UI передана как Frontend-зависимость.

## История BE-01/BE-02 и BE-03

- Статус: **BE-01/BE-02 реализованы и проверены**; результаты BE-03 и ограничения
  готовности реальных данных приведены в отдельном разделе ниже.
- Владелец: Backend по прямому поручению пользователя; импорт и read-only ревью
  делегированы субагентам с отдельными областями ответственности.
- База первого этапа BE-01/BE-02: `main`, commit `4afad787bc338f93763d91c1aeaf5042f57244c3`.
- Рабочая копия: `/home/ziyadin/Documents/ChatGPT/ekt-backend-be01-be02-bTm8Du`.
- Режим первой передачи: отдельный клон, detached HEAD, без коммитов и push.
  После передачи пользователь явно поручил опубликовать готовые изменения.
  Для этого выбрана существующая `main` того же клона; feature-ветка не создавалась.
  Коммит с этим handoff предназначен для отправки в `origin/main` без переписывания
  истории. Фактический SHA публикации проверяется по удалённой ветке.
- Patch первой передачи: `/home/ziyadin/Documents/ChatGPT/ekt-backend-be01-be02-bTm8Du.patch`.
  Он сохранён как локальный артефакт этапа до разрешения публикации; для получения
  опубликованной версии следует использовать коммит из `origin/main`.
- Исходный checkout `/home/ziyadin/Documents/ChatGPT/Hack` был на `98333ed6418b75952499ac6192a599c01b281bea`;
  он и его незакоммиченные каталоги оставлены без изменений.

## Файлы BE-01/BE-02

1. `src/ekt/ingest.py` — BE-01, присутствие пустой таблицы и проверка заголовков.
2. `src/ekt/application.py` — BE-02, сопоставление содержимого и provenance.
3. `tests/backend/test_empty_import.py` — 23 проверки импорта.
4. `tests/backend/test_provenance.py` — 26 проверок ручного редактирования.
5. `docs/INPUT_GUIDE.md` — правила очистки и сохранения происхождения.
6. `docs/handoffs/backend.md` — эта передача.

`schema.py`, общие fixtures, UI, расчётные формулы, зависимости, конфигурация,
общие STATUS/TASKS/INTERFACES/DEVELOPMENT не изменены.

## BE-01: отсутствующая и пустая таблицы

Раньше `merge_tables` пропускал любую пустую таблицу. Теперь канонический импорт
отмечает реально найденные таблицы приватным `DataFrame.attrs["_ekt_canonical_table"]`
с именем таблицы. `merge_tables` заменяет непустые таблицы как раньше, а пустые —
только с соответствующей отметкой. Созданные `Bundle` отсутствующие таблицы такой
отметки не имеют. Отдельных полей Bundle и дополнительных колонок не добавлено;
публичные сигнатуры сохранены.

CSV/ZIP/XLSX с заголовками `inbound` без строк очищает inbound и сохраняет sales,
если sales не присутствует в файле. Повторные файлы применяются последовательно:
последняя присутствующая версия таблицы побеждает, включая пустую. Ошибка в любом
файле не изменяет предыдущий Bundle.

Для явной очистки нужны хотя бы заголовки `REQUIRED[name]` из существующей схемы.
Полный набор SCHEMAS подходит. Пустой файл, пустой лист и недостаточные заголовки
вызывают ValueError. Непустые неполные таблицы по-прежнему разрешены для дальнейшего
редактирования; полная валидация выполняется при расчёте.

Отметка проверена через `Bundle.copy()` и `normalize()` на зафиксированной версии
pandas. Это внутренняя метаинформация импорта в памяти. Пересоздание DataFrame без
attrs не сохраняет команду очистки. Обычный вручную созданный пустой Bundle, как
и прежде, ничего не очищает при merge; для очистки используйте канонический импорт
с заголовками либо `edit_table` с пустой таблицей.

## BE-02: стратегия идентификации

Ключ сопоставления — точный кортеж **всех нормализованных значений записи без
PROVENANCE**, включая дополнительные бизнес-столбцы. Набор колонок берётся из
обеих таблиц, порядок колонок фиксируется; пропуски сравниваются единообразно.
Позиция и pandas index не участвуют. Бизнес-KEY/документ сами по себе недостаточны:
несколько продаж одного SKU в одном документе могут быть разными записями.

Все четыре поля provenance сохраняются только при единственном совпадении в
исходной и сохранённой таблице. Новые/изменённые записи и неоднозначные дубли
получают `source_file=manual`, `data_mode=manual`, `source_sheet=<таблица>`;
source_row заполняется обычным normalize для ручного набора. При дублировании
manual получают все неразличимые копии. Если из прежних одинаковых строк с разными
источниками остаётся одна, она также manual. Количества и строки не суммируются
и не удаляются автоматически. Присланные редактором поля provenance игнорируются.

Новые ID или бизнес-поля не введены, Bundle и интерфейс UI не менялись. Сравнение
выполняется по точному содержимому без отдельного ID. Эквивалентные представления
чисел, дат, boolean и пропусков сравниваются после существующего normalize.

Сохранена возможность backend-клиента исправить или очистить невалидную raw
таблицу: сначала нормализуется исправленный набор. Если исходную таблицу нельзя
нормализовать, все сохранённые строки остаются manual. Неверный новый ввод
по-прежнему вызывает ошибку без мутации входов. Этот случай найден независимым
ревью, исправлен и покрыт четырьмя дополнительными регрессиями.

**Предел без постоянного ID:** удаление B и изменение A до полного содержимого B
неотличимо от сохранения B. Метод подтверждает совпадение данных, а не физическую
историю записи. Если нужен журнал таких действий, потребуется отдельное
согласование стабильных ID/журнала с интегратором. В этом этапе они не добавлялись.

## Проверки BE-01/BE-02

Linux, Python 3.12.14, отдельная `.venv`; 44 зависимости установлены из неизменённого
`requirements.lock` командой `uv pip install --python .venv/bin/python -r requirements.lock`.

| Команда / этап | Фактический результат |
|---|---|
| До изменений: `.venv/bin/python -m pytest -q` | **54 passed in 17.31s** |
| После финального исправления, активированная .venv: `python -m pytest tests/backend -q` | **55 passed in 8.93s** |
| После финального исправления, активированная .venv: `python -m pytest -q` | **103 passed in 23.45s** |
| После финального исправления: `.venv/bin/python scripts/smoke.py` | synthetic, 4 позиции / 2 поставщика, manual_zero=0, approval=approved, CSV 6406 bytes / XLSX 8533 bytes |
| `git diff --check` | Без ошибок |

Backend-тесты проверяют CSV/ZIP/XLSX, отсутствие таблицы, заголовки без строк,
copy/normalize, последовательные файлы, неизменность входов при успехе/ошибке,
перестановку, удаление, дубли с разными источниками, индекс с повторами,
дополнительные поля, канонические типы и исправление raw input.

Независимое read-only ревью дополнительно проверило настоящий текст `"<NA>"`
против пропуска и несколько циклов copy/normalize. Интерактивный браузер отдельно
не запускался; существующие frontend-тесты прошли в полном наборе.

## BE-03: проверка оригиналов

Пользователь предоставил Google Docs с описанием кейса и оригинальные IEK.zip /
Systeme electric.zip, затем явно поручил BE-03. Перед работой выполнены fetch и
fast-forward; исходная база проверки — `874238a4f6e054d8eea4ebd71faf41516f7226d4`.
Перед финальными тестами подтянут новый `origin/main`
`800b4c852b65a742d84317b9ae861806690dc337` (LOG-02); пересечения файлов не было.
Затем получен `a9decc75dac1f904c8930ac38cbd81f39c93e9a6` с изменениями DATA/D1–D3.
Его изменения сохранены; исправление имени и новая документация BE-03 совмещены
поверх них. Проверки повторены для объединённого адаптера.
Последняя интеграционная база — `c876046b74c20d580330e2cde84f5764563e64df`:
CI, общая документация и дополнительный ENGINE-тест. Продуктовый код в этом
коммите не изменился; полный набор тестов повторён после fast-forward.
Отдельно пользователь подтвердил отсутствие подтверждений складского пула,
единиц пути и смысла MOQ IEK: неизвестные значения следует сохранять.

Оба ZIP скачаны, их CRC проверены. Приватные оригиналы, манифест источников,
скрипты независимого аудита и подробные сверки находятся вне репозитория:
`/home/ziyadin/Documents/ChatGPT/ekt-be03-private-buzUUm/`.
Никаких исходных записей, SKU, количеств операций или диагностических выгрузок
в коммит не добавлено.

| Оригинал | SHA-256 | Покрытие | Первый полный импорт |
|---|---|---|---|
| IEK.zip | `fed312f909943e69b17d2cc5dd577123b5fe0874064558e8f503deaf9fc50054` | 6 XLSX / 6 листов | 80.963 с, успешно |
| Systeme electric.zip | `10ce24d572b750e199bfe8aa9887d467f67471950804392dccc97715dd0344cc` | 6 XLSX / 8 листов | 34.43 с, успешно |

Время измерено на Linux/Python 3.12.14 при параллельной проверке; это наблюдение
данной машины, не гарантия производительности. Независимое чтение исходных книг
заняло 19.002 с для IEK и 9.16 с для Systeme. Архивы читались полностью в памяти;
исходные XLSX не изменялись и не пересчитывались.

Параметры: `report_date="2026-09-22"` по именам отчётов о пути,
`confirmed_scope=None`, `inbound_base_units=False`, `iek_moq_meaning="unknown"`.
Дата не объявляется независимо подтверждённой для каждой книги или актуальной
на другой день. Агрегаты остались `UNSPECIFIED`, реальные склады динамики
сохранены, путь — `pending` с исходным количеством и неизвестным `qty_base_unit`.

### Что сверено

- Все 12 XLSX / 14 листов прочитаны; дополнительные листы сезонных агрегатов
  Systeme не стали дополнительными строками SKU. Все шесть типов отчётов каждого
  поставщика распознаны.
- Независимая проверка исходных клеток подтвердила строковые SKU, знаки и
  количества, даты, единицы, склад динамики, типы документов и происхождение
  импортированных записей. Месячные пустоты не превращаются в наблюдаемые нули.
- IEK month_start остаётся историческим; актуальный остаток не придуман.
  Systeme AX/AY/AZ сверены напрямую; резерв из available повторно не вычитался.
  Партии и их исходные количества сверены с IEK D:I и Systeme BC.
- Сезонные исходные коэффициенты/кеши формул сохранены в raw_factor, проверена
  нормализация 12 коэффициентов. Сомнительные AP/AQ/AR/AS не используются как
  достоверный готовый прогноз. Excel-формулы не пересчитывались.
- Выполнены независимые выборки по 12 SKU/месяцам для каждого поставщика.
  У IEK арифметически совпали 3 выборки, у 9 найдены расхождения; у Systeme
  расхождения обнаружены во всех 12. Склады не объединялись. Поскольку область
  месячного источника не подтверждена, ни совпадение, ни расхождение не является
  доказательством бухгалтерской сопоставимости; подробности оставлены приватно.

### Исправление и обнаруженные ограничения

**Исправлено в BE-03:** месячные остатки Systeme имеют заголовки A «№», B «Номенклатура»,
C «Номенклатура.Код». Адаптер брал название из A и мог перезаписывать имя товара
порядковым номером. Теперь колонка имени определяется по заголовку перед кодом;
сохраняется совместимость с IEK (имя в A) и прежним макетом A «Наименование». Добавлены отдельные
синтетические регрессии обоих поставщиков и проверки исходных названий в opt-in
тестах. Формулы расчёта, схема Bundle и UI не менялись.
Независимое ревью проверило выбор правильной колонки имени на всех строках
stock-отчётов: несовпадений не найдено.

**Исходные данные требуют исправления:** в динамике IEK — 18 пустых количеств,
Systeme — 13. В полном импорте до `a9decc7` неизвестные сохранены, `validate`
возвращает ошибку `sales.quantity_signed`. Для Systeme это подтверждено и на
последней базе; для IEK новая проверка останавливается раньше на MOQ.
Полный набор пока нельзя выдать за готовый к расчёту.
Строки не удалялись, количества нулями не заменялись.

**Новый строгий отказ IEK:** после интеграции DATA `a9decc7` импорт выявил
15 ошибок Excel `#N/A` / `#Н/Д` в колонке E MOQ. До этого они молча превращались
в неизвестный `unconfirmed_moq`. Это реально недопустимые исходные значения,
а не регрессия, которую следует обходить: строгая проверка сохранена.
Нужен исправленный MOQ-файл. Независимое повторение отказа заняло 5.85 с;
детали и адреса исходных ячеек сохранены только в приватной папке.

**Соседние задачи не присваивались:** в Systeme подтверждены противоречащие
месячные значения monthly/V2. Их диагностика, поддержка форматов ETA и строгая
проверка нечисловых значений поступили от DATA в `a9decc7`; BE-03 эти алгоритмы
не меняет.
Дополнительно обнаружен дефект DATA/D3: у 26 товаров Systeme месячная кратность
перезаписывает отличающееся значение MOQ, но `quantity_rule_source` продолжает
ссылаться на MOQ. До `a9decc7` независимый синтетический пример также подтвердил
зависимость от порядка файлов. DATA добавил сортировку файлов, но она не задаёт
семантический приоритет MOQ и не обновляет источник кратности.
Нужны согласованный приоритет источников, корректное происхождение
поля и сообщение конфликта. Отдельный opt-in тест объединённого MOQ помечен
`xfail(strict=True)` только для Systeme: это известный дефект, не успешная проверка.
Самостоятельный импорт каждой MOQ-книги проверяется отдельно без этой отметки.
Разбор исходных расхождений, подтверждение scope/единиц/MOQ и получение актуального
остатка IEK остаются необходимыми условиями практического расчёта. Отсутствующие
customer_id, stockout и политики не восстановлены из документов или месячных нулей.

### Воспроизводимость

Изменённые файлы этапа: `src/ekt/partner.py`,
`tests/backend/test_partner_report_headers.py`, `tests/backend/test_partner_originals.py`,
`docs/INPUT_GUIDE.md`, `docs/DATA_LIMITATIONS.md`, этот handoff.
Локальный diff этапа для интегратора:
`/home/ziyadin/Documents/ChatGPT/ekt-be03-c876046.patch` (база `c876046`).
После получения опубликованного коммита повторно применять этот patch не нужно.

Публичный набор содержит синтетические тесты; проверки оригиналов запускаются
отдельно и читают приватную папку с `IEK.zip` и `Systeme.zip`:

```bash
EKT_PARTNER_ARCHIVE_DIR=/path/to/private/originals python -m pytest tests/backend/test_partner_originals.py -q --tb=short
python -m pytest tests/backend -q
python -m pytest -q
python scripts/smoke.py
```

Без переменной окружения 18 проверок оригиналов явно пропускаются. Их пропуск
не является свидетельством проверки реальных данных. Тесты не публикуют исходные
значения при несовпадении. Детальные приватные скрипты позволяют повторить полную
сверку, а opt-in тесты проверяют компактную выборку, семантику и ограничения.

Проверки на промежуточной базе `800b4c8`, Linux / Python 3.12.14:

| Команда | Фактический результат |
|---|---|
| Opt-in проверка оригиналов | **17 passed, 1 xfailed in 129.73s**; ожидаемый дефект DATA/D3 описан выше |
| `python -m pytest tests/backend -q` | **59 passed, 18 skipped in 9.33s** |
| `python -m pytest -q` | **181 passed, 18 skipped in 73.91s** |
| `python scripts/smoke.py` | synthetic, 4 позиции / 2 поставщика, manual_zero=0, approval=approved; оба экспорта сформированы |

Повторные проверки после интеграции DATA `a9decc7`:

| Команда | Фактический результат |
|---|---|
| `python -m pytest tests/backend -q` | **59 passed, 18 skipped in 9.05s** |
| `python -m pytest -q` | **211 passed, 18 skipped in 73.59s** |
| Opt-in проверка оригиналов | **8 passed, 1 xfailed, 9 errors in 41.12s**; все девять ошибок относятся к общему fixture импорта IEK, это один отказ импорта, а не девять независимых дефектов |
| `python scripts/smoke.py` | synthetic, 4 позиции / 2 поставщика, manual_zero=0, approval=approved; оба экспорта сформированы |

Зелёный общий набор без оригиналов не закрывает отказ реального импорта IEK.
Ошибки opt-in оставлены видимыми, не заменены пропусками или успешными проверками.
На итоговой базе `c876046` повторный `python -m pytest -q` дал
**212 passed, 18 skipped in 72.73s**. В этом обновлении добавлен один ENGINE-тест,
адаптер не менялся; результат проверки оригиналов на `a9decc7` остаётся применим.

Read-only ревью дополнительно проверило все строки stock-отчётов, формат исходных
дат и воспроизвело конфликт кратности на синтетике. Проверено скрытие деталей
исключений opt-in тестов; контрольные суммы оригиналов после работы неизменны.

Техническая проверка предоставленных оригиналов выполнена с замечаниями.
Приёмка успешного полного импорта на последней базе остаётся заблокированной
исходными ошибками IEK; исправление конфликтующего MOQ Systeme принадлежит DATA.
Готовность реальных заказов, точность прогноза, интеграция 1С и официальная сдача
этим не подтверждены.

## Действия интегратора

При получении коммита из `origin/main` повторно применять patch не нужно.
Для отдельной копии исходной базы доступен patch первой передачи:

```bash
git apply --check /home/ziyadin/Documents/ChatGPT/ekt-backend-be01-be02-bTm8Du.patch
git apply /home/ziyadin/Documents/ChatGPT/ekt-backend-be01-be02-bTm8Du.patch
python -m pytest tests/backend -q
python -m pytest -q
python scripts/smoke.py
```

Patch содержит новые тесты и handoff первого этапа до разрешения публикации.
При переносе файла на другой компьютер путь к patch следует заменить.

INTERFACES/DEVELOPMENT и статусы BE-01/BE-02 уже обновлены интегратором в `c876046`.
Следующая передача: обновить TASKS/STATUS по результатам BE-03 и зарегистрировать
дополнительный дефект происхождения MOQ Systeme для DATA. Архивы теперь получены;
фактические блокеры — 15 ошибок Excel MOQ IEK, пропуски количеств продаж,
конфликтующая кратность Systeme и неподтверждённые бизнес-входы.
Публикация кода разрешена прямым поручением пользователя; деплой и официальная
сдача не выполнялись.

## BE-03/MOQ: значение и происхождение правила закупки

Это новая запись этапа; предыдущие результаты BE-03 выше относятся к прежним SHA.

- Поручение: пользователь разрешил исправление конфликта кратности/источника,
  относящегося к Backend, с сохранением формул Logic.
- Исполнитель и координатор подзадачи: Backend `/root`, сессия
  `01a0cdb3-55e7-7ed1-961b-3f7e65b54335`; синтетические тесты делегированы
  `quantity_source_tests`, opt-in проверки — `quantity_contract_review`.
- База после fetch/fast-forward: `a3efa609b08a114381a61a2ee294cc28bec12f65`.
- Рабочая копия: `/home/ziyadin/Documents/ChatGPT/ekt-backend-be01-be02-bTm8Du`,
  существующий отдельный клон, `main`; Git выполняет только `/root`.
- Назначенные файлы: `src/ekt/partner.py`,
  `tests/backend/test_partner_quantity_sources.py`,
  `tests/backend/test_partner_originals.py`, `docs/INPUT_GUIDE.md`, этот handoff.
- Общие файлы, Bundle, schema.py, UI, зависимости и Logic не меняются.
  Обновление общих TASKS/STATUS/DATA_LIMITATIONS передаётся интегратору.
- Приёмка: конфликт известной кратности должен быть явно отклонён независимо
  от порядка файлов; при успехе ссылка указывает на ячейку сохранённого числа.
  Пустоты остаются неизвестными, ноль не теряется, семантика IEK сохраняется.
- Статус подзадачи: дефект импорта исправлен и проверен; общая приёмка BE-03
  по реальным данным остаётся заблокированной исходными значениями.

### Правило импорта

Адаптер собирает все известные кандидаты количества по SKU и полю. Systeme
получает `order_multiple` из MOQ E и monthly D; IEK сохраняет выбранную пользователем
семантику `unknown` / `minimum` / `multiple`. Различающиеся значения останавливают
импорт с ValueError: поставщик, SKU, поле, оба значения, файлы, листы и строки.
Значения выводятся без округления коротким форматом, чтобы близкие числа оставались
различимы. Никакой источник не объявляется авторитетным без подтверждения.

Совпадающие кандидаты выбирают источник детерминированно: MOQ перед monthly,
затем файл/лист/строка. Приоритет влияет только на ссылку для одинакового числа.
Значение и существующее `quantity_rule_source` (`filename:row`) назначаются вместе.
Пустой кандидат не создаёт и не меняет правило или источник. Публичные поля и
формат ссылки не расширены; новые ID не введены.

### Проверки и ограничения этапа

До исправления причинные синтетические регрессии дали **14 failed, 6 passed in
1.35s**. После исправления новый набор вместе с существующими проверками адаптеров
и заголовков дал **59 passed in 2.77s**. Оригиналы не исправлялись и не фильтровались.

Финальные результаты на Linux / Python 3.12.14:

| Команда | Фактический результат |
|---|---|
| `python -m pytest tests/backend -q` | **79 passed, 20 skipped in 9.99s** |
| `python -m pytest -q` | **251 passed, 20 skipped in 92.77s** |
| `EKT_PARTNER_ARCHIVE_DIR=/path/to/private/originals python -m pytest tests/backend/test_partner_originals.py -q --tb=short` | **2 passed, 18 skipped in 42.88s** |
| `python scripts/smoke.py` | synthetic, 4 позиции / 2 поставщика, manual_zero=0, approval=approved; CSV/XLSX сформированы |
| `git diff --check`, ссылки изменённых документов | Без ошибок |

20 новых синтетических сценариев проверяют порядок ZIP/имён файлов, одинаковые
и конфликтующие источники, пустоты, ноль, дроби, близкие числа, дубли, разные SKU
и три режима MOQ IEK. До исправления 14 сценариев воспроизводили дефект.

Две успешные проверки оригиналов подтверждают **безопасный отказ**: IEK из-за
настоящих ошибок Excel в MOQ, Systeme из-за независимо найденных противоречащих
кратностей. Текст ошибки сверяется с исходными значениями и точными источниками
в памяти. 18 зависимых проверок Bundle явно пропущены, поскольку целый архив
не импортирован; это не успешная приёмка реальных данных. Неожиданная ошибка или
принятие архива с известным конфликтом по-прежнему проваливают тест. Старый `xfail`
удалён. Без приватной папки пропускаются все 20 opt-in проверок.

Успешная ветка этих проверок дополнительно проверена на синтетических шести
отчётах обоих поставщиков и на случае пустого MOQ/известного monthly. После
исправления оригиналов проверки успешного Bundle возобновятся автоматически.
Read-only ревью подтвердило атомарность числа/источника, детерминизм, сохранение
нулей и приватность стандартного вывода pytest.

Остаётся получить исправленные 15 ячеек Excel MOQ IEK, 31 количество продаж
(18 IEK / 13 Systeme), разрешить противоречащие значения кратности Systeme и
подтвердить бизнес-входы. Формулы Logic и неизвестные параметры не менялись.

Интегратору: отразить исправление кода в TASKS/STATUS/DATA_LIMITATIONS; вместо
старого дефекта ложного источника теперь действует явная блокировка противоречащих
входных значений. Исторические результаты выше сохранены с их исходными SHA.
Patch этапа: `/home/ziyadin/Documents/ChatGPT/ekt-be03-moq-a3efa60.patch`,
база `a3efa609b08a114381a61a2ee294cc28bec12f65`. После получения опубликованного
коммита повторно применять patch не нужно. Push проверяется по удалённому SHA
и сообщается в итоговом ответе.
