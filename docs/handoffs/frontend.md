# Передача Frontend — UI-LOGIC-01

- Дата: 23.09.2026; статус: локальные проверки завершены, передано Reviewer.
- Задача и назначение: [UI-LOGIC-01](../tasks/UI-LOGIC-01.md).
- Исполнители: `/root/risk_ui` — представление риска;
  `/root` (сессия `01a0cdfc-d3c8-7c30-82fd-b27597a5c11e`) — детектор,
  карточка, документация Frontend и интеграция. Git выполняет интегратор `/root`.
- Общее дерево `/home/ziyadin/Documents/ChatGPT/Hack`, main. Исходная база:
  `f099198ea6c2d54ded05f020d8ae28be3316df31`. Согласованный CI-only переход к
  `016c2cfbf34f84c916de594ab779f8c5bbd26666`; все финальные проверки на нём + UI-diff.
- Файлы: src/ekt_ui/presentation.py, layout.py, results.py, review.py, details.py;
  tests/frontend/test_risk_integration.py, test_detector_integration.py;
  docs/DASHBOARD.md, docs/handoffs/frontend.md. Общие документы ведёт интегратор.

## Поведение и совместимость

classify_risk получает неизменённые строки Calculation.rows и config.as_of.
Единый mapping используется таблицей и карточкой: unavailable → «Нужны данные»,
calculated/unknown → «Риск не определён», critical → «Критично», risk или нужный
заказ → «Пополнение», none без заказа → «Норма». KPI/фильтр дефицита учитывают
только critical/risk; положительный страховой заказ сам по себе риска не означает.
Перепроверка включает warnings/unavailable/unknown. Цвет unknown добавлен в обе
карты цветов, чтобы новый статус не вызывал KeyError.

detector_status получает нормализованные продажи только одного KEY, config.as_of
и config.remove_oneoffs. Отключение и короткая история отображаются отдельно.
При evaluated наличие DemandResult подтверждает выполненную проверку; без него
показывается только применимость и недоступность результата. Незавершённый заказ
из-за отсутствия остатка не скрывает уже выполненный расчёт спроса.
Найденные события и реально применённые исключения считаются раздельно по applied;
пустой список не доказывает отсутствие аномалий. Предупреждения — в «Источниках».

Формулы Logic, контракты, CSV/XLSX и количества не менялись. Риск не зависит от
ручной правки; row_id, ноль, сброс утверждения и экспорт всего выбранного набора
сохраняются. UI не вычитает события и не восстанавливает спрос повторно.

## Проверки и ограничения

- Профильные риск/presentation: 29 passed; detector: 9 passed.
- Frontend/AppTest: 66 passed, 88.80s; полный pytest: 301 passed, 20 skipped,
  116.82s. Python 3.12.14/Linux, отдельная среда по requirements.lock.
- Smoke: synthetic, 4 позиции/2 поставщика, manual_zero=0, approved,
  CSV 6406 / XLSX 8532 байт. Проверка зависимостей и diff без ошибок.
- Реальный браузер localhost:18502, desktop 1440×1000 и mobile 390×844:
  demo/расчёт/KPI/таблица/аналитика, фильтр «Критично», evaluated/0 найденных,
  disabled, синтетическая короткая история 11/12; предупреждения не обрезаются.
  Unknown, границы 6/7 и смешанный applied дополнительно проверены AppTest.
- Оригиналы в этом этапе не проверялись, 20 opt-in пропусков требуют приватных
  архивов. Удалённый CI не запускался, действует внешний billing lock/CI-02.
  Windows, нагрузка и чистая копия приложения отдельно не проверялись.
- Публикацию после независимого ревью выполняет интегратор. Самоссылочный SHA
  не записывается в собственный коммит; итоговый SHA подтверждается по remote.
  Деплой, официальная сдача и отправка заказов не выполнялись.

# Frontend: FE-04 / Inspo

## Назначение

- Прямое поручение пользователя: подобрать дизайн через Inspo и улучшить существующий Streamlit.
- Исполнитель Frontend и интегратор публикации: Codex, текущая сессия прямого поручения пользователя «Дизайн через Inspo».
- Рабочая папка: `C:\Users\User\OneDrive\Документы\ChatGPT\Хакалаем`, существующий `main`, без новых веток.
- Начальная база после проверки origin: `8179244b9a967e27e1b44c30f3d230ebcb62fbb2`.
- Зависимости: завершённый FE-03 и существующие API Bundle/Calculation/Approval. Нового контракта или зависимости нет.
- Назначенные файлы: `.streamlit/config.toml`, `src/ekt_ui/app.py`, `imports.py`, `layout.py`, `quality.py`, `results.py`, `review.py`, `styles.css`, `tests/frontend/test_dashboard.py`, `docs/DASHBOARD.md`, этот handoff. Имена модулей без каталога относятся к `src/ekt_ui/`.
- Интеграционные документы в рамках того же поручения: `docs/TASKS.md`, `docs/STATUS.md`.
- Пересечения с активными локальными правками не обнаружены; Backend/Logic не менялись исполнителем. Полученные изменения команды сохранены отдельным merge.

## Результат

- UI-коммит `65a7c43`: нейтральная зелёная тема по Clay/Amie, сгруппированная боковая панель, адаптивные фильтры, виды таблицы, сброс фильтров, видимые explanation.
- Источники и способ вызова hosted Inspo MCP описаны в [DASHBOARD](../DASHBOARD.md). Глобальная установка и передача данных проекта не выполнялись.
- Ручной ноль, причины правок, утверждение и состав экспорта сохраняются при смене вида и сбросе фильтров. Поставщик виден в обоих видах таблицы.
- Изменения команды `a3efa60` и `398065e` включены merge-коммитом `ba56bb7e701e1ab11346f3ac9f4a62c5a22d31c4`, без конфликтов и переписывания истории.
- Diff UI: `git show 65a7c43`; общий API, формулы, адаптеры и экспорт в этом diff не меняются.

## Проверки

- До интеграции: `python -m pytest tests/frontend -q -p no:cacheprovider`: **42 passed in 128.20s**.
- Полный предварительный прогон: **233 passed, 18 skipped in 177.15s**.
- После последнего уточнения таблицы: `python -m pytest tests/frontend/test_dashboard.py -q -p no:cacheprovider`: **10 passed in 51.11s**.
- На интегрированной базе `ba56bb7`: `python -m pytest -q -p no:cacheprovider`: **253 passed, 20 skipped in 188.92s**. Приватные opt-in проверки не запускались без папки оригиналов.
- После включения Logic `f099198`, итоговая кодовая база `44f868510e77ab69b18a4e98089e925573385175`: полный pytest **277 passed, 20 skipped in 203.95s**; smoke: 4 позиции, 2 поставщика, manual_zero=0, approved, CSV 6406 / XLSX 8495 байт.
- Browser/Playwright: desktop 1440×1000, tablet 762×1000, mobile 390×844; демо, расчёт, оба вида таблицы, фильтр поставщика и сброс, аналитика и графики. Выход страницы по горизонтали не обнаружен, таблица прокручивается внутри контейнера. Исправлены отступ от верхнего меню и двухколоночные фильтры на среднем экране.
- Итог общего прогона после интеграции и публикация фиксируются интегратором в [STATUS](../STATUS.md) и ответе пользователю.

## Границы

Приёмка оригиналов в этой задаче не проводилась. Новые цены, ABC/XYZ, хранение
сессий, отправка заказов и интеграция 1С не имитируются. Чистая установка и
нагрузочное тестирование не повторялись; зависимости не менялись. Отдельный
независимый Reviewer в этой задаче не назначался. CSS зависит от DOM Streamlit,
поэтому обновление библиотеки требует повторной визуальной проверки.


## UI-02 — темы, старт и представления (23.09.2026)

- Исполнитель Frontend / интегратор: Codex `/root`, сессия
  `01a0ce1a-209b-73c1-9da8-684b13a451e1`, общее дерево Windows/main.
- База: `54f9bff8ef59f2866cfe680d5c3ace91737f5efd`; Git/TASKS/STATUS переданы AUD-01.
- Статус: реализовано и проверено. Commit/push выполняет эта же сессия;
  подтверждённый SHA — в итоговом сообщении, без самоссылки в коммите.
- Файлы продукта: `.streamlit/config.toml`, `src/ekt_ui/app.py`, `results.py`,
  `review.py`, `styles.css`, новый `workspace.py`; тесты: новый
  `tests/frontend/test_workspace.py`. Документы: DASHBOARD, TASKS, STATUS,
  tasks/UI-02 и этот handoff. Полные пути заданы в назначении UI-02.
- Сделано: две палитры и системный режим через нативное меню, адаптивный старт,
  шаги, быстрые фильтры, сохранение/применение/удаление восьми представлений,
  компактные строки, отдельный scope. Портальные диалоги используют нативную тему.
- Проверки: весь pytest — **308 passed / 20 skipped**; отдельно 68 прежних
  Frontend и 5 новых тестов; smoke — 4 строки / 2 поставщика, manual_zero=0,
  approved, CSV/XLSX. Desktop 1280×720 и mobile 390×844 в браузере: обе темы,
  демо/расчёт, сохранение представления, таблица, аналитика; горизонтального
  переполнения документа на 390px нет. Подробные команды в tasks/UI-02.
- Контракт прежний; отсутствующие сохранённые фильтры не расширяют выборку.
  Новые AppTest проверяют неизменность ручного нуля, Approval и полного экспорта.
- Ограничения: представления только на время сессии; оригиналы не проверены;
  D01/D10/D11 и полный D09 остаются по аудиту. Новых числовых обещаний нет.
  В DASHBOARD подготовлены задания Backend (хранилище) и Logic (совместимость
  черновика / сравнение сценариев); это пока предложения для назначения.
