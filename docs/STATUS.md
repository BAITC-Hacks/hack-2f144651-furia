# Состояние EKT — 23.09.2026

## Интеграция Logic в интерфейс — UI-LOGIC-01

- Подключены существующие classify_risk и detector_status. Таблица, карточка,
  KPI и фильтры разделяют недоступный расчёт, неизвестный риск, критичность,
  пополнение и норму. Дата берётся из config.as_of; заказ и риск независимы.
- Детектор получает продажи одного supplier/SKU/scope и настройку расчёта.
  Показаны disabled, n из 12 и выполненная применимая эвристика. Пустые events
  не объявляются отсутствием аномалий; найденные события и applied-исключения
  различаются, предупреждения движка сохранены.
- Числа, формулы, Python/Streamlit, правки по row_id, ручной ноль, утверждение
  и глобальный состав CSV/XLSX не изменены. Добавлены 26 поведенческих тестов.
- Свежая база f099198 получена до параллельного этапа. CI-02 по согласованию
  завершил публикацию своих CI/docs на `016c2cfbf34f84c916de594ab779f8c5bbd26666`
  и явно передал Git/общие документы этому интегратору. Чужие файлы сохранены.
- На 016c2cf + UI-diff: Linux/Python 3.12.14, Frontend/AppTest **66 passed**;
  полный pytest **301 passed, 20 skipped**, 116.82s; smoke — 4 synthetic позиции,
  2 поставщика, manual_zero=0, approved, CSV/XLSX; pip check успешен.
- Браузер: desktop 1440×1000 и mobile 390×844, таблица/фильтры/карточка,
  состояния evaluated/disabled/11 из 12. Mobile-сообщения переносятся, горизонтальный
  размер документа и карточки 390px. Независимый Reviewer: 91 тест и отдельные
  числовые примеры, продуктовых замечаний нет; финальный отчёт в handoff.
- Подробности, файлы, исполнители и ограничения: [UI-LOGIC-01](tasks/UI-LOGIC-01.md),
  [Frontend](handoffs/frontend.md), [Reviewer](handoffs/review.md).
- 20 opt-in проверок оригиналов пропущены; это не сверка партнёрских данных.
  Удалённый CI не запускался и не считается зелёным; действует CI-02 ниже.
  Windows/нагрузка/чистая копия приложения отдельно не проверялись.
- Локальная приёмка завершена; публикация выполняется после финального ревью
  документации и сверки remote. Подтверждённый SHA сообщается после push.
  Деплой, официальная сдача и отправка заказов не выполнялись.

## Временный ручной режим GitHub Actions — CI-02

- По просьбе пользователя убраны автоматические запуски `.github/workflows/checks.yml`
  при push и pull request. Сохранены ручной запуск, обе ОС и все проверки.
- Причина красных статусов подтверждена GitHub API для запуска
  [35853319926](https://github.com/BAITC-Hacks/hack-2f144651-furia/actions/runs/35853319926):
  billing lock владельца аккаунта, обе jobs отклонены без выполнения шагов.
  У текущего аккаунта есть push, но нет admin/maintain.
- Это временная приостановка автопроверок. Биллинг остаётся внешним блокером;
  отсутствие статуса у нового коммита не является успешным CI. Прежние запуски
  и их результаты сохраняются. Порядок восстановления — в [README](../README.md#проверки).
- Назначение, согласованные базы и фактические проверки: [CI-02](tasks/CI-02.md).
- В чистой Linux-копии `f099198ea6c2d54ded05f020d8ae28be3316df31`,
  Python 3.12.14 и зависимости из requirements.lock: **275 passed, 20 skipped**;
  pip check, smoke и синтетическая оценка прогноза успешны. Пропуски — приватные
  opt-in проверки оригиналов. Windows этого этапа не проверен. YAML валиден;
  все jobs и параметры вне триггеров совпадают с базой.
- Изменение опубликовано в `origin/main` коммитом
  `9eb895c6076045cf9800e4d53192be4aa8504ac2`; удалённый SHA подтверждён.
  GitHub API для этого SHA: 0 workflow runs, 0 check runs, 0 commit statuses;
  опубликованный workflow имеет только ручной триггер. Независимый Reviewer
  не нашёл блокирующих замечаний: [отчёт](handoffs/review.md).

## Унификация ролей — ROLE-01

- По прямому поручению пользователя инструкции приведены к Frontend / Backend /
  Logic; интегратор координирует общие изменения, Reviewer проверяет результат.
- База получена через `git fetch origin` и fast-forward до
  `8179244b9a967e27e1b44c30f3d230ebcb62fbb2`; история и локальные файлы сохранены.
- Единственная таблица владельцев — [AGENT_PLAN](AGENT_PLAN.md), актуальные задачи
  и назначения — [TASKS](TASKS.md). Стартовая инструкция, промпты, вложенные AGENTS
  и описание интерфейсов используют эту схему. Прежний план сохранён в архиве,
  старые промпты содержат только указатели; исторические handoff не переписывались.
- Подтверждено постоянное правило: перед задачей проверять свежий main, после
  каждого завершённого и проверенного изменения, включая документацию, делать
  commit/push. В общем дереве это выполняет один интегратор для всех подзадач.
- Изменения только в Markdown. Продуктовый код, Python API и тесты не менялись;
  результаты прежних прогонов ниже не являются проверкой этого этапа.
- Назначения, критерии приёмки и итог проверки: [ROLE-01](tasks/ROLE-01.md).
  Проверены 40 Markdown-файлов, 163 относительные ссылки и 6 якорей; у всех
  47 Python-файлов один владелец, `git diff --check` без ошибок. Независимый
  Reviewer проверил инструкции; замечание об авторе личного handoff исправлено.
  Продуктовые тесты в этой документационной задаче не запускались.
- Commit/push выполняются по постоянному поручению; фактический удалённый SHA
  и успешность публикации подтверждаются в итоговом сообщении задачи.

## История продуктовых этапов

Ниже сохранены результаты на указанных в них базах. Старые названия ролей,
назначения и разрешения относятся к своим этапам; действуют AGENT_PLAN и
[правила Git](../AGENTS.md#git). Актуальные ограничения продукта описаны в
[DATA_LIMITATIONS](DATA_LIMITATIONS.md), открытые задачи — в TASKS.

## Обновление Интерфейса Закупщика

- По поручению пользователя сохранён Streamlit. Работа начата после fast-forward
  до `874238a`, перед публикацией получены все командные изменения до `8ac2345`.
  Новые ветки не создавались, история и origin сохранены.
- FE-03: адаптивный dashboard, KPI, поставщики/поиск/фильтры, редактируемая таблица
  с месячной историей, боковая аналитика, очереди данных и поставок, пакетные действия.
  Формулы и контракты редизайн не меняет. [Подробности и предложения](DASHBOARD.md).
- Правки хранятся по row_id между фильтрами; ручной ноль, причины, сброс утверждения,
  сохранение набора при ошибке импорта и глобальный состав экспорта покрыты тестами.
- Windows / Python 3.12.14, итоговая база `8ac2345` + UI-diff:
  `python -m pytest -q -p no:cacheprovider` — **231 passed, 18 skipped in 157.42s**.
  Приватные opt-in тесты пропущены без EKT_PARTNER_ARCHIVE_DIR; это не сверка оригиналов.
- Отдельный `python -m pytest tests/frontend -q -p no:cacheprovider` до получения
  последних Backend-коммитов — **40 passed in 118.04s**; те же 40 тестов включены
  в итоговый полный прогон. Cacheprovider отключён из-за локальных прав на кеш pytest.
- `python scripts/smoke.py` на итоговой базе: synthetic, 4 позиции / 2 поставщика,
  manual_zero=0, approval=approved; CSV 6406 / XLSX 8496 байт.
  `python -m pip check`: No broken requirements found. `git diff --check`: без ошибок.
- В браузере проверены desktop 1440×1000, mobile 390×844 и средний размер окна:
  демо/расчёт, таблица, аналитика и графики, контроль данных, поставки, утверждение
  и меню экспорта. Исправлены прозрачный фон drawer и горизонтальный выход панели
  действий. Локальный сервер текущей main-копии: http://127.0.0.1:8501/.
- Цена/валюта/ABC/XYZ отсутствуют в контракте и не имитируются. Утверждение только
  сессионное, экспорты не отправляются поставщику или в 1С. Нагрузочная проверка не проведена.
- Получен Backend-handoff `af32ab3`: оригиналы у команды уже есть, сверка выявила
  ошибки исходных значений IEK и конфликт происхождения MOQ Systeme. Подробности
  и воспроизводимые opt-in результаты — в [handoff](handoffs/backend.md).
  Старые записи ниже об отсутствии оригиналов относятся к предыдущим этапам.
- Удалённый CI остаётся заблокированным billing issue, описанным ниже; зелёный
  удалённый запуск этого редизайна не заявляется. Чистая установка ниже проверялась
  на прежнем SHA, в этом UI-этапе она не повторялась.
- В AGENTS закреплено прямое правило пользователя: перед задачей обновлять main,
  после проверенных изменений делать commit/push. Деплой и официальная сдача не выполняются.

Ниже сохранена история предыдущих проверок, а не результаты нового интерфейса.

## Актуальный итог завершения задач

- Доступные задачи FE-01/02, BE-01/02, LOG-01/02, D1/D2/D3, E1–E4 и
  локальная интеграция L2–L5 завершены. Таблица: [TASKS.md](TASKS.md).
- Код проверен на `c876046b74c20d580330e2cde84f5764563e64df` в чистой копии
  `git archive` без .git/кэшей и в новой `.venv`, установленной из requirements.lock.
- Windows / Python 3.12.14: **208 passed in 68.97s**, pip check без ошибок.
  Smoke: 4 позиции / 2 поставщика, ручной ноль=0, approved, CSV 6406 / XLSX 8496 байт.
  Два walk-forward JSON побайтово одинаковы. Streamlit в чистой копии стартовал,
  health=ok; временный процесс остановлен. Пользовательский путь проверен AppTest,
  отдельная интерактивная браузерная проверка в этом этапе не проводилась.
- INT-01: workflow готов и отправлен, но удалённая приёмка заблокирована владельцем
  аккаунта GitHub. [Запуск 35850350803](https://github.com/BAITC-Hacks/hack-2f144651-furia/actions/runs/35850350803)
  остановлен до первого шага на Windows и Linux. Дословная причина GitHub:
  “The job was not started because your account is locked due to a billing issue.”
  Нужна разблокировка биллинга и повторный запуск. Это не зелёный CI;
  Linux текущей версии отдельно не проверен. Настройки биллинга не менялись.
- BE-03/D4: оригиналы IEK/Systeme и подтверждения складов/единиц отсутствуют,
  реальная сверка и точность не проверены. Синтетика не заменяет эти входы.
- Коммиты этапов в main: `800b4c8` (LOG-02), `a9decc7` (D1–D3), `c876046`
  (CI, E3-регрессия, актуализация интерфейсов/задач). Финальная запись результатов
  меняет только документацию; точный итоговый SHA доступен в git log.
- Независимый внешний REVIEWER не назначался; проведена локальная приёмка
  интегрированной копии. Деплоя, отправки поставщикам и официальной сдачи не было.

## Завершение backlog по новому поручению пользователя

- Актуальная база получена из main: `874238a4f6e054d8eea4ebd71faf41516f7226d4`.
- LOG-02 реализован: проверка полного множества уникальных row_id до объединения;
  неизвестные/пропущенные/пустые ID отклоняются. Ручной ноль и текущий UI сохранены.
- Проверки review/workflow/frontend: 36 passed. Остальные этапы и финальный полный
  прогон фиксируются ниже по мере завершения. План: [tasks-closeout.md](tasks-closeout.md).
- После LOG-02 полный pytest: **177 passed, 68.79s**; smoke: 4 позиции,
  2 поставщика, ручной ноль и approved, CSV/XLSX сформированы.
- LOG-02 отправлен в main коммитом `800b4c8`. D1/D2/D3: исправлены конфликты
  месячных источников, даты ETA и молчаливое исчезновение неверных чисел/дат.
  Оригиналы по-прежнему не предоставлены; проверяются только синтетические макеты.
- После D1/D2/D3: полный pytest **207 passed, 69.40s**; smoke успешен,
  `python -m pip check` — No broken requirements found. ETA с двузначным годом
  и диапазоном дат остаются неизвестными до уточнения.
- Исправления импорта отправлены коммитом `a9decc7`. Добавлен CI для Python 3.12
  Windows/Linux: отдельная venv, locked install, pip check, pytest, smoke,
  синтетический walk-forward. Итог первого удалённого запуска указан выше.
- Дополнительная числовая E3-регрессия: 10/день × 21 день × 1.5 = 315,
  safety=30, заказ=345; три теста engine_regressions прошли. Production-формулы не менялись.
- Общие TASKS/INTERFACES/DEVELOPMENT/README приведены в соответствие с уже
  интегрированными FE/BE/LOG изменениями. Для финального L5 установлена новая
  изолированная среда из requirements.lock; проверка чистой копии следует далее.
- Пользователь поручил завершить все доступные задачи, перед работой обновлять main,
  после завершённых изменений делать commit/push. Remotes не меняются, force не используется.
- Записи ниже сохранены как история предыдущих этапов; старые planned/числа тестов
  не являются актуальным статусом интегрированной версии.

## Прямое Внесение Изменений В Main

- По прямому указанию пользователя коммит разделения архитектуры перенесён поверх командных изменений из актуального `origin/main`; исходные коммиты handoff сохранены.
- Frontend: `src/ekt_ui/app.py`; Backend операции с данными: `src/ekt/application.py`; вычислительные модули остаются в `src/ekt/`. Запуск через `app.py` прежний.
- Добавлены вложенные AGENTS, карта файлов/контрактов/истории и backlog задач Frontend/Backend/Logic. Правило прямой работы Codex в main отражено в корневых инструкциях и README.
- После переноса на main: `git switch main`, fast-forward до `origin/main`, cherry-pick архитектурного коммита; история не переписывалась.
- Проверки актуального дерева: `.venv/Scripts/python.exe -m pytest -q` — **54 passed in 34.17s**; `scripts/smoke.py` — 4 позиции / 2 поставщика, оба экспорта и approved; `pip check` — ошибок нет.
- Переносный commit и SHA зафиксированы в git log; изменения пушатся в main по явному поручению пользователя.
- Реальные архивы партнёров не предоставлены; интеграция 1С не проверена.

## Текущий этап: подготовлена передача ролям

Официальный repo: `BAITC-Hacks/hack-2f144651-furia`.
Исходная база этого этапа: `f2f0ac1f77393b413464c5435971b42ed0d505c0`.
Полный SHA версии, которую получает исполнитель, фиксируется командой
`git rev-parse HEAD` после коммита изменений; не выдавайте исходную базу за новую версию.

- Сохранён сквозной MVP: импорт → расчёт → объяснение → корректировка → утверждение → CSV/XLSX.
- Исправлен числовой тип unconfirmed_moq; неподтверждённая семантика MOQ по-прежнему блокирует точный заказ. Добавлены две регрессии общей схемы.
- Добавлены START_HERE, AGENT_PLAN, реальные INTERFACES, четыре ролевых промпта и шаблон handoff; устаревшее указание «один агент / без remote» заменено.
- README дополнен запуском Linux/macOS и передачей агентам. Восстановлена отсутствовавшая ссылка на SOURCES в перенесённом кейсе.
- Отдельный read-only аудит подтвердил примеры D1/D2/E1/E2. Эти дефекты включены в задания, **ещё не исправлены**. Проверка ролевых инструкций не нашла конфликтов владения.

## Роли и ближайшие задачи

| Роль | Статус назначения | Следующий результат |
|---|---|---|
| DATA | Не назначен; промпт готов | D1/D2: конфликты месячных источников, форматы ETA; затем D3/D4 |
| ENGINE | Не назначен; промпт готов | E1/E2: повторяемость клиента и предупреждение короткой истории; затем E3 |
| LEAD | Подготовка общей базы выполнена текущим интегратором | Назначить исполнителей, передать один SHA и продолжить L2–L5 |
| REVIEWER | Не назначен; запуск после интеграции | Независимая приёмка согласованного SHA |

Подробные разрешённые файлы и критерии — [AGENT_PLAN.md](AGENT_PLAN.md).
Исполнитель пишет только собственный handoff; общий STATUS обновляет LEAD.

## Фактические проверки текущего этапа

После объединения с `0ea01e6`: Linux / Python 3.12.14, `python -m pytest -q` → **48 passed**, 14.19 s; `python scripts/smoke.py` → synthetic, 4 позиции / 2 поставщика, manual_zero=0, approval=approved, оба экспорта сформированы. Относительные Markdown-ссылки объединённой версии проверены. Ниже сохранены проверки до объединения.

- Linux, Python 3.12.14; 44 зависимости из неизменённого requirements.lock установлены в отдельную среду через uv.
- Исходный f2f0ac1: `python -m pytest -q` → **37 passed, 1 failed** (unconfirmed_moq).
- После исправления и двух регрессий: `python -m pytest -q` → **40 passed**, 13.59 s. Включает M1–M5 на синтетике, импорт макетов и Streamlit AppTest.
- В чистой копии передаваемых исходников без .git/кэшей/зависимостей: `uv venv --python 3.12 .venv`, `uv pip install --python .venv/bin/python -r requirements.lock`, `.venv/bin/python -m pytest -q` → **40 passed**, 15.56 s.
- Из чистой копии запущен Streamlit на локальном проверочном порту 18501: health=ok; процесс остановлен после проверки. Это проверка старта сервера, пользовательский путь отдельно покрыт AppTest.
- Относительные ссылки во всех 17 Markdown-файлах проверены, отсутствующих целей нет; `git diff --check` без ошибок.
- Это не полная независимая приёмка и не проверка оригиналов. Интерактивный браузер в текущем этапе не проверялся; UI проверен AppTest.

Запуск Windows: `.venv\Scripts\python.exe -m streamlit run app.py`.
Linux/macOS: `.venv/bin/python -m streamlit run app.py`. Адрес: `http://127.0.0.1:8501`.
Установка и команды проверки — [README](../README.md).

## Ограничения и публикация

- Оригинальные ZIP партнёра в этом клоне не предоставлены. Все 12 схем проверяются на синтетических макетах; сверка оригиналов не выполнена.
- Демо: 4 позиции / 2 поставщика, явно synthetic. Клиенты, stockout, сроки и текущий остаток из отсутствующих полей не выдумываются.
- Остались конкретные D/E/L задачи; зелёный текущий набор тестов не подтверждает их завершение. Backtest качества на реальных данных отсутствует.
- Утверждение живёт в сессии. Экспорт не является отправкой поставщику или проверенной интеграцией с 1С.
- Пользователь явно разрешил отправку текущих изменений в официальный repo. История и origin сохранены; деплой не разрешён. Фактическую публикацию проверять по удалённому SHA.

## История предыдущего этапа

В предыдущем STATUS другой исполнитель зафиксировал отдельное разрешение пользователя
на commit/push 23.09.2026 в 14:03 Asia/Tashkent и браузерную проверку на Windows.
Исходные коммиты приложения: bf5df48, 19f0ec8; объединение с историей команды: f2f0ac1.
Подготовительный комплект первоначально использован на 527b300; для новых ролей
изучен 3d53907. Прошлое разрешение не выдаётся за постоянное разрешение на новые публикации.

## Дополнительная работа команды, полученная перед публикацией

Перед push получены и объединены коммиты `541a8c4` и `0ea01e6`. Сохранены дополнительные числовые поля и проверки схемы, консервативный расчёт, UI-фильтры, setup.ps1, smoke.py и документация команды. Запись её фактических проверок ниже сохранена как история; это не повторная проверка Windows текущим интегратором.

### Отчёт команды на 0ea01e6

## Историческая Проверка MVP

Ниже запись предыдущей среды; её серверы, разрешения и результаты не являются текущими автоматически.

- Этап 3 завершён, 23.09.2026 14:18 (UTC+5): работает полный MVP M1–M5; чистая установка и сквозной сценарий подтверждены.
- Режим: локальная разработка. В 14:03 Asia/Tashkent 23.09.2026 пользователь отдельно разрешил сделать коммиты, push текущего состояния и продолжить работу. Деплой и отправка данных внешним сервисам не разрешены.
- Источник инструкций: agent-kit, commit 527b3004c01da60b0e63b10e0d2aaa3304dcd0ba.
- Исходные ZIP партнёра отсутствуют. Демо и все Excel-макеты тестов явно синтетические.
- Среда: Python 3.12.14, зависимости установлены в `.venv`, версии зафиксированы в `requirements.lock`.
- Запуск: `.venv\Scripts\python.exe -m streamlit run app.py`; интерфейс `http://127.0.0.1:8501` открыт и расчёт проверен в браузере.
- Проверки в рабочей среде: `.venv\Scripts\python.exe -m pytest -q` — **46 passed in 11.50s**. Проверены T1–T12/M1–M5, импорт, возвраты, SKU, ETA, кратность, области/снимки, пропуски, объяснение, ручной ноль, CSV/XLSX, отмена утверждения, Streamlit AppTest.
- `.venv\Scripts\python.exe scripts/smoke.py`: synthetic, 4 позиции, 2 поставщика, CSV 6406 bytes / XLSX 8495 bytes, manual_zero=0, approval=approved.
- `.venv\Scripts\python.exe -m pip check`: No broken requirements found.
- Чистая копия commit `541a8c4f5faeb8034828309bfe65bc975d734237`: `git archive` → новая папка → `.\setup.ps1` без предварительной `.venv` → `pytest -q`: **46 passed in 13.11s**; smoke: 4 позиции / 2 поставщика / manual_zero=0 / approved; pip check: ошибок нет.
- Сервер чистой копии запущен на 8502, health ответил `ok`; временный процесс после проверки остановлен. Рабочий интерфейс на 8501 оставлен запущенным.
- Реальный браузер: загрузка демо, расчёт, фильтры, утверждение, изменение 90 → 0, отмена прежнего утверждения, ввод причины, повторное утверждение; кнопки CSV/XLSX доступны. Детали — `docs/VERIFICATION.md`.
- Демо: 4 позиции / 2 поставщика. Обнаружен разовый клиентский заказ 1050 единиц; восстановлено 58.238095 единиц за синтетический stockout.
- Добавлены адаптеры шести типов отчётов каждого поставщика, явное подтверждение области склада/единиц и семантики MOQ, фильтры склада/категории. Исправлены тип MOQ IEK и нормализация prior с pandas 3.
- Подготовлены README, инструкция импорта, методология, ограничения, сценарий демонстрации и setup.ps1.
- Git: исходный commit команды c04f6c5 сохранён. Первый разрешённый push в main подтверждён для f2f0ac1; merge включает локальные bf5df48 и 19f0ec8. Remotes не добавлялись/не изменялись; push выполнен по URL.
- Архивов партнёра нет: 12 схем Excel проверяются только на явно синтетических макетах; реальная сверка не выполнена.
- Следующий шаг: после получения реальных архивов сверить адаптеры/единицы/остатки и параметры с партнёром. До этого реальные точность, скорость на полном каталоге и интеграция 1С остаются непроверенными.
