# Завершение доступного backlog

- Статус: доступные доработки завершены; BE-03/D4 и удалённая приёмка INT-01 blocked внешними условиями.
- Исполнитель: Codex, интеграция оставшихся задач по поручению пользователя «закончи все».
- Ветка: main; исходная актуальная база `874238a4f6e054d8eea4ebd71faf41516f7226d4`.
- Уже реализованы: FE-01/FE-02, BE-01/BE-02, LOG-01, E1/E2/E4. Требуется актуализировать общие статусы и проверить интеграцию.
- Выполненный объём: LOG-02 (review.py и tests/test_review.py), D1/D2/D3 (partner.py и независимые регрессии), INT-01 (код CI), E3/L2–L5 (локальные проверки и документация).
- Общий контракт: формы Bundle/Calculation и UI не меняются. Правки менеджера обязаны содержать ровно одну строку на каждый row_id расчёта, включая невыбранные; недостающие/лишние/пустые/повторные ID — ValueError. Это сохраняет текущий UI и исключает молчаливую потерю строк.
- Конфликты основных месячных источников блокируют импорт; основной отчёт выше V2, различия видны в notes. Неизвестные даты и отсутствующие поля не выдумываются.
- Приёмка: причинные тесты дефектов, полный pytest, smoke, pip check, чистая установка Python 3.12, проверка CI и обновлённые TASKS/STATUS/INTERFACES/README.
- BE-03/D4 остаются blocked до предоставления оригинальных архивов, подтверждения областей/единиц и реальных значений. Интеграция 1С и официальная сдача не заявляются.
- Git: перед работой получен актуальный main; завершённые этапы коммитятся и отправляются по постоянному поручению пользователя, без изменения remotes/истории.

## Этапы

- LOG-02: `800b4c8`, отправлен в main. Полный pytest 177 passed; отдельные review/workflow/frontend 36 passed, smoke успешен.
- D1/D2/D3: перед исправлением 17 из 29 новых проверок воспроизводили дефекты. Основной monthly=100 / V2=999 → выбрано 100 с явным сообщением на каждый месяц. Два разных основных значения → ошибка. Добавлены Excel/ISO ETA, неизвестный переход года, непустые нечисловые значения и ошибки дат с контекстом файла. Обновлён INPUT_GUIDE; схема Bundle не менялась.
- После D1/D2/D3 и дополнительной проверки двузначного года: 207 passed / 69.40s; smoke и pip check успешны.
- D1/D2/D3 отправлены в `a9decc7`. CI и общие документы — в `c876046`; checkout/setup-python закреплены проверенными SHA v7, permissions только contents:read, проверки на синтетике.

## Финальная воспроизводимая проверка

База чистой копии: `c876046b74c20d580330e2cde84f5764563e64df`. Исходники получены
через git archive без рабочей .venv и кэшей; новая среда Python 3.12.14 установлена
из неизменённого requirements.lock. Существующая рабочая среда не копировалась.

| Команда | Результат |
|---|---|
| `python -m pip install -r requirements.lock` в новой .venv | Успешно |
| `python -m pip check` | No broken requirements found |
| `python -m pytest -q` в чистой копии | 208 passed, 68.97s |
| `python scripts/smoke.py` | 4 позиции, 2 поставщика, manual_zero=0, approved, CSV/XLSX |
| `python -m scripts.evaluate_forecast --demo --output evaluation.json` дважды | Побайтово одинаковые JSON |
| Streamlit из чистой копии на временном localhost-порту | health=ok, собственный процесс остановлен |

E3 покрыт test_acceptance/test_edges/test_forecast/test_engine_regressions:
пересечения и частичные stockout, отсутствие доступных дней, месячная сверка,
незавершённый месяц/cutoff, резерв, ETA, MOQ и неизвестные входы. Новая числовая
проверка единственного роста: 10×21×1.5=315, safety=30, заказ=345.
UI-путь и оба поставщика проверены AppTest и smoke. Отдельный внешний REVIEWER
не назначался; эти результаты не выдаются за независимый внешний аудит.

## Что требует внешнего действия

1. **BE-03/D4**: передать реальные архивы и подтверждения складов, единиц,
   конверсий и MOQ. После этого выполнить сверку и измерить реальную точность/производительность.
2. **INT-01**: GitHub Actions run 35850350803 не начал ни одного шага на обоих OS:
   `The job was not started because your account is locked due to a billing issue.`
   Владелец аккаунта должен решить вопрос биллинга; затем перезапустить
   [EKT checks](https://github.com/BAITC-Hacks/hack-2f144651-furia/actions/runs/35850350803).
   Workflow реализован, но удалённый CI и Linux-прогон не подтверждены. Биллинг не изменялся.
